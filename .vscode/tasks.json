{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Setup SystemMonitor",
            "type": "shell",
            "command": "cmd /c",
            "args": [
                "if not exist .venv (echo Creating virtual environment... && python -m venv .venv) && .venv\\Scripts\\pip.exe install -r requirements.txt --quiet && echo Dependencies ready!"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}